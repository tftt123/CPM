# 前端开发规范

> Vue 3 + TypeScript + Element Plus，制造业B端系统风格。
> **此文档与Java/PG版本基本一致，只有API地址和少量规范调整。**

---

## 项目目录结构

```
cpm-web/
├── public/
├── src/
│   ├── api/                    # 接口请求（按模块分文件）
│   │   ├── auth.ts
│   │   ├── customer.ts
│   │   ├── quotation.ts
│   │   ├── sample.ts
│   │   ├── procurement.ts
│   │   ├── kanban.ts
│   │   └── request.ts          # Axios实例
│   ├── assets/                 # 静态资源
│   ├── components/             # 公共组件
│   │   ├── AppHeader.vue       # 顶部导航
│   │   ├── AppSidebar.vue      # 左侧菜单
│   │   ├── DataTable.vue       # 通用表格（分页+搜索）
│   │   ├── SearchForm.vue      # 通用搜索表单
│   │   ├── FileUpload.vue      # 文件上传
│   │   ├── StatusTag.vue       # 状态标签
│   │   └── ApprovalTimeline.vue # 审批记录时间线
│   ├── views/                  # 页面（按模块分目录）
│   │   ├── login/
│   │   │   └── LoginView.vue
│   │   ├── dashboard/
│   │   │   └── DashboardView.vue
│   │   ├── customer/
│   │   │   ├── CustomerList.vue
│   │   │   └── CustomerForm.vue
│   │   ├── quotation/
│   │   │   ├── OpportunityList.vue
│   │   │   ├── OpportunityForm.vue
│   │   │   └── QuotationDetail.vue
│   │   ├── sample/
│   │   │   ├── SampleRequestList.vue
│   │   │   ├── SampleRequestForm.vue
│   │   │   └── BatchManage.vue
│   │   ├── procurement/
│   │   │   ├── ProcRequestList.vue
│   │   │   └── ProcOrderList.vue
│   │   ├── kanban/
│   │   │   └── KanbanBoard.vue
│   │   └── expedite/
│   │       └── ExpediteList.vue
│   ├── router/
│   │   └── index.ts            # 路由配置
│   ├── stores/                 # Pinia状态管理
│   │   ├── user.ts             # 用户信息
│   │   ├── app.ts              # 应用状态（侧边栏折叠等）
│   │   └── tagsView.ts         # 多标签页
│   ├── utils/
│   │   ├── auth.ts             # Token存取
│   │   ├── date.ts             # 日期格式化
│   │   └── permission.ts       # 权限检查
│   ├── types/                  # TypeScript类型定义
│   │   ├── customer.ts
│   │   ├── quotation.ts
│   │   └── common.ts
│   ├── App.vue
│   └── main.ts
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

---

## 路由设计

```typescript
// router/index.ts
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  {
    path: '/login',
    component: () => import('@/views/login/LoginView.vue'),
    meta: { public: true }
  },
  {
    path: '/',
    component: () => import('@/views/layout/MainLayout.vue'),
    redirect: '/dashboard',
    children: [
      { path: 'dashboard', component: () => import('@/views/dashboard/DashboardView.vue'), meta: { title: '首页' } },
      { path: 'customer', component: () => import('@/views/customer/CustomerList.vue'), meta: { title: '客户管理' } },
      { path: 'product', component: () => import('@/views/product/ProductList.vue'), meta: { title: '产品管理' } },
      { path: 'quotation/opportunity', component: () => import('@/views/quotation/OpportunityList.vue'), meta: { title: '商机管理' } },
      { path: 'quotation/detail/:id', component: () => import('@/views/quotation/QuotationDetail.vue'), meta: { title: '报价详情' } },
      { path: 'sample/request', component: () => import('@/views/sample/SampleRequestList.vue'), meta: { title: '样品申请' } },
      { path: 'sample/batch', component: () => import('@/views/sample/BatchManage.vue'), meta: { title: '批次管理' } },
      { path: 'procurement/request', component: () => import('@/views/procurement/ProcRequestList.vue'), meta: { title: '采购申请' } },
      { path: 'kanban', component: () => import('@/views/kanban/KanbanBoard.vue'), meta: { title: '样品看板' } },
    ]
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// 路由守卫
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  if (!to.meta.public && !token) {
    next('/login')
  } else {
    next()
  }
})

export default router
```

---

## 菜单设计（按角色）

### 销售角色菜单

```
首页
客户管理
产品管理
商机管理（报价流程）
样品申请
样品看板
送样管理
```

### 技术角色菜单

```
首页
样品申请（查看）
批次管理
生产计划
样品看板
```

### 采购角色菜单

```
首页
采购申请
采购订单
供应商管理
样品看板（查看）
```

### 主管角色菜单

```
首页
审批中心（待办事项）
样品看板
报表统计
```

---

## 通用表格组件（最常用）

几乎每个列表页都是：搜索表单 + 数据表格 + 分页 + 操作按钮。

### 列表页模板

```vue
<template>
  <div class="page-container">
    <!-- 搜索区 -->
    <el-form :model="query" inline>
      <el-form-item label="客户名称">
        <el-input v-model="query.keyword" placeholder="搜索客户" clearable />
      </el-form-item>
      <el-form-item>
        <el-button type="primary" @click="handleSearch">搜索</el-button>
        <el-button @click="handleReset">重置</el-button>
        <el-button type="success" @click="handleAdd">新增</el-button>
      </el-form-item>
    </el-form>

    <!-- 表格 -->
    <el-table :data="tableData" v-loading="loading" border>
      <el-table-column type="index" label="序号" width="60" />
      <el-table-column prop="customerCode" label="客户编码" />
      <el-table-column prop="customerName" label="客户名称" />
      <el-table-column prop="industry" label="行业" />
      <el-table-column prop="contactName" label="联系人" />
      <el-table-column label="操作" width="200">
        <template #default="{ row }">
          <el-button type="primary" size="small" @click="handleEdit(row)">编辑</el-button>
          <el-button type="danger" size="small" @click="handleDelete(row)">删除</el-button>
        </template>
      </el-table-column>
    </el-table>

    <!-- 分页 -->
    <el-pagination
      v-model:current-page="query.pageNum"
      v-model:page-size="query.pageSize"
      :total="total"
      layout="total, prev, pager, next"
      @change="loadData"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { customerApi } from '@/api/customer'

const loading = ref(false)
const tableData = ref([])
const total = ref(0)
const query = ref({
  pageNum: 1,
  pageSize: 10,
  keyword: ''
})

const loadData = async () => {
  loading.value = true
  const res = await customerApi.list(query.value)
  tableData.value = res.data.list
  total.value = res.data.total
  loading.value = false
}

const handleSearch = () => {
  query.value.pageNum = 1
  loadData()
}

const handleReset = () => {
  query.value.keyword = ''
  handleSearch()
}

onMounted(loadData)
</script>
```

---

## 状态标签颜色规范

所有页面统一状态颜色：

```vue
<!-- StatusTag.vue -->
<template>
  <el-tag :type="tagType">{{ label }}</el-tag>
</template>

<script setup lang="ts">
import { computed } from 'vue'

const props = defineProps<{ status: number; type: 'quotation' | 'sample' | 'batch' | 'procurement' }>()

const config: Record<string, Record<number, { label: string; type: string }>> = {
  quotation: {
    0: { label: '草稿', type: 'info' },
    1: { label: '待评审', type: 'warning' },
    2: { label: '待审批', type: 'warning' },
    3: { label: '已发出', type: 'success' },
    9: { label: '完成', type: 'success' }
  },
  sample: {
    0: { label: '草稿', type: 'info' },
    1: { label: '待审批', type: 'warning' },
    2: { label: '已批准', type: 'success' },
    3: { label: '已驳回', type: 'danger' },
    9: { label: '完成', type: 'success' }
  },
  batch: {
    0: { label: '待排产', type: 'info' },
    1: { label: '计划中', type: '' },
    2: { label: '生产中', type: 'primary' },
    3: { label: '已完成', type: 'success' }
  },
  procurement: {
    0: { label: '无需采购', type: 'info' },
    1: { label: '待申请', type: 'warning' },
    2: { label: '采购中', type: 'primary' },
    3: { label: '已到货', type: 'success' },
    4: { label: '异常', type: 'danger' }
  }
}

const tagType = computed(() => config[props.type][props.status]?.type || 'info')
const label = computed(() => config[props.type][props.status]?.label || '未知')
</script>
```

---

## API 请求封装

```typescript
// api/request.ts
import axios from 'axios'
import { ElMessage } from 'element-plus'

const request = axios.create({
  baseURL: 'http://localhost:5001/api',  // .NET后端默认端口
  timeout: 15000
})

// 请求拦截器：加token
request.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = 'Bearer ' + token
  }
  return config
})

// 响应拦截器：统一处理错误
request.interceptors.response.use(
  response => {
    const data = response.data
    if (data.code !== 200) {
      ElMessage.error(data.message || '请求失败')
      return Promise.reject(data)
    }
    return data
  },
  error => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      window.location.href = '/login'
    } else {
      ElMessage.error(error.message || '网络错误')
    }
    return Promise.reject(error)
  }
)

export default request
```

```typescript
// api/customer.ts
import request from './request'

export const customerApi = {
  list: (params: any) => request.get('/customer/list', { params }),
  getById: (id: number) => request.get(`/customer/${id}`),
  add: (data: any) => request.post('/customer', data),
  update: (id: number, data: any) => request.put(`/customer/${id}`, data),
  delete: (id: number) => request.delete(`/customer/${id}`)
}
```

---

## 页面布局规范

### 整体布局

```
┌─────────────────────────────────────────┐
│  Logo    事必得 CPM系统          🔔 👤  │  ← AppHeader
├──────────┬──────────────────────────────┤
│          │                              │
│  首页    │                              │
│  客户管理│      页面内容区域              │  ← RouterView
│  商机管理│                              │
│  样品看板│                              │
│          │                              │
└──────────┴──────────────────────────────┘
   ↑ AppSidebar
```

### MainLayout.vue 框架

```vue
<template>
  <el-container class="layout-container">
    <el-aside width="200px">
      <AppSidebar />
    </el-aside>
    <el-container>
      <el-header>
        <AppHeader />
      </el-header>
      <el-main>
        <router-view />
      </el-main>
    </el-container>
  </el-container>
</template>
```

---

## 新手开发顺序（每阶段页面清单）

### P1 用户登录
1. `LoginView.vue` — 登录表单（账号、密码、登录按钮）
2. `MainLayout.vue` — 主布局（侧边栏+顶部+内容区）
3. `AppSidebar.vue` — 根据角色显示不同菜单

### P2 基础数据
1. `CustomerList.vue` — 客户列表（表格+分页+搜索）
2. `CustomerForm.vue` — 客户表单（新增/编辑弹窗）
3. `ProductList.vue` — 产品列表
4. `ProductForm.vue` — 产品表单

### P3 报价流程
1. `OpportunityList.vue` — 商机列表（显示阶段、截止日倒计时）
2. `OpportunityForm.vue` — 创建商机
3. `QuotationDetail.vue` — 报价详情页（显示技术评审区、审批区、操作按钮）

### P4 样品申请
1. `SampleRequestList.vue` — 样品申请列表
2. `SampleRequestForm.vue` — 发起样品申请（关联商机、上传文件）
3. `SampleRequestDetail.vue` — 样品申请详情（显示审批状态）

### P5 批次管理
1. `BatchManage.vue` — 批次列表（可展开显示所属申请单）
2. `BatchSplit.vue` — 批次拆分向导（一个申请单拆多个批次）

### P6 采购管理
1. `ProcRequestList.vue` — 采购申请列表
2. `ProcOrderList.vue` — 采购订单跟踪

### P7 看板
1. `KanbanBoard.vue` — 看板页面（分状态栏+卡片+筛选器）

（P8-P9以此类推）