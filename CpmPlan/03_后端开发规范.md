# 后端开发规范（.NET 8）

> ASP.NET Core Web API + EF Core + JWT + 自研状态机

---

## 项目结构

```
CpmServer/
├── CpmServer.csproj
├── Program.cs                    # 入口+服务注册+中间件管道
├── appsettings.json
├── appsettings.Development.json
├── Common/                       # 公共模块
│   ├── ApiResult.cs              # 统一响应包装
│   ├── ApiResult{T}.cs           # 泛型响应
│   ├── PagedResult{T}.cs         # 分页响应
│   ├── BusinessException.cs      # 业务异常
│   └── GlobalExceptionHandler.cs # 全局异常处理中间件
├── Models/                       # 实体类（EF Core）
│   ├── SysUser.cs
│   ├── SysRole.cs
│   ├── SysUserRole.cs
│   ├── CrmCustomer.cs
│   ├── QuoOpportunity.cs
│   ├── QuoQuotation.cs
│   └── ...
├── Data/
│   └── CpmDbContext.cs           # EF Core DbContext
├── DTOs/                         # 数据传输对象
│   ├── Auth/
│   │   ├── LoginRequest.cs
│   │   └── LoginResponse.cs
│   ├── Customer/
│   │   ├── CustomerCreateDto.cs
│   │   └── CustomerListDto.cs
│   └── Quotation/
│       ├── QuotationCreateDto.cs
│       └── TechReviewDto.cs
├── Services/                     # 业务逻辑层
│   ├── IAuthService.cs
│   ├── AuthService.cs
│   ├── ICustomerService.cs
│   ├── CustomerService.cs
│   ├── IQuotationService.cs
│   └── QuotationService.cs
├── Controllers/                  # API控制器
│   ├── AuthController.cs
│   ├── CustomerController.cs
│   └── QuotationController.cs
├── Hubs/                         # SignalR实时推送
│   └── KanbanHub.cs
├── Jobs/                         # Hangfire定时任务
│   └── OverdueCheckJob.cs
└── Middleware/                   # 自定义中间件
    └── RequestTimingMiddleware.cs
```

---

## 统一响应格式

所有接口返回统一JSON：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### ApiResult.cs

```csharp
namespace CpmServer.Common;

public class ApiResult
{
    public int Code { get; set; }
    public string Message { get; set; } = string.Empty;
    public object? Data { get; set; }

    public static ApiResult Success(object? data = null)
        => new() { Code = 200, Message = "success", Data = data };

    public static ApiResult Error(string message)
        => new() { Code = 400, Message = message };

    public static ApiResult Error(int code, string message)
        => new() { Code = code, Message = message };
}

public class ApiResult<T>
{
    public int Code { get; set; }
    public string Message { get; set; } = string.Empty;
    public T? Data { get; set; }

    public static ApiResult<T> Success(T? data)
        => new() { Code = 200, Message = "success", Data = data };

    public static ApiResult<T> Error(string message)
        => new() { Code = 400, Message = message };
}
```

### PagedResult.cs

```csharp
namespace CpmServer.Common;

public class PagedResult<T>
{
    public List<T> List { get; set; } = new();
    public long Total { get; set; }
    public int PageNum { get; set; }
    public int PageSize { get; set; }

    public static PagedResult<T> Of(List<T> list, long total, int pageNum, int pageSize)
        => new() { List = list, Total = total, PageNum = pageNum, PageSize = pageSize };
}
```

---

## Controller 写法规范

```csharp
using CpmServer.Common;
using CpmServer.DTOs.Customer;
using CpmServer.Services;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;

namespace CpmServer.Controllers;

[ApiController]
[Route("api/[controller]")]
[Authorize]  // 默认需要登录
public class CustomerController : ControllerBase
{
    private readonly ICustomerService _customerService;

    public CustomerController(ICustomerService customerService)
    {
        _customerService = customerService;
    }

    // 查询列表（分页+搜索）
    [HttpGet("list")]
    public async Task<ApiResult<PagedResult<CustomerListDto>>> List(
        [FromQuery] int pageNum = 1,
        [FromQuery] int pageSize = 10,
        [FromQuery] string? keyword = null)
    {
        var result = await _customerService.GetListAsync(pageNum, pageSize, keyword);
        return ApiResult<PagedResult<CustomerListDto>>.Success(result);
    }

    // 查询详情
    [HttpGet("{id}")]
    public async Task<ApiResult<CustomerListDto>> GetById(long id)
    {
        var result = await _customerService.GetByIdAsync(id);
        return ApiResult<CustomerListDto>.Success(result);
    }

    // 新增
    [HttpPost]
    public async Task<ApiResult<long>> Add([FromBody] CustomerCreateDto dto)
    {
        var id = await _customerService.AddAsync(dto);
        return ApiResult<long>.Success(id);
    }

    // 修改
    [HttpPut("{id}")]
    public async Task<ApiResult> Update(long id, [FromBody] CustomerCreateDto dto)
    {
        await _customerService.UpdateAsync(id, dto);
        return ApiResult.Success();
    }

    // 删除
    [HttpDelete("{id}")]
    public async Task<ApiResult> Delete(long id)
    {
        await _customerService.DeleteAsync(id);
        return ApiResult.Success();
    }
}
```

**URL规范**：
- 列表：`GET /api/customer/list?pageNum=1&pageSize=10`
- 详情：`GET /api/customer/{id}`
- 新增：`POST /api/customer`
- 修改：`PUT /api/customer/{id}`
- 删除：`DELETE /api/customer/{id}`
- 操作：`POST /api/quotation/{id}/submit-review`

---

## Service 写法规范

```csharp
using CpmServer.Common;
using CpmServer.Data;
using CpmServer.DTOs.Customer;
using CpmServer.Models;
using Microsoft.EntityFrameworkCore;

namespace CpmServer.Services;

public interface ICustomerService
{
    Task<PagedResult<CustomerListDto>> GetListAsync(int pageNum, int pageSize, string? keyword);
    Task<CustomerListDto?> GetByIdAsync(long id);
    Task<long> AddAsync(CustomerCreateDto dto);
    Task UpdateAsync(long id, CustomerCreateDto dto);
    Task DeleteAsync(long id);
}

public class CustomerService : ICustomerService
{
    private readonly CpmDbContext _db;

    public CustomerService(CpmDbContext db)
    {
        _db = db;
    }

    public async Task<PagedResult<CustomerListDto>> GetListAsync(int pageNum, int pageSize, string? keyword)
    {
        var query = _db.Customers.AsNoTracking().AsQueryable();

        if (!string.IsNullOrWhiteSpace(keyword))
        {
            query = query.Where(c => c.CustomerName.Contains(keyword) 
                                  || c.CustomerCode.Contains(keyword));
        }

        var total = await query.CountAsync();
        var list = await query
            .OrderByDescending(c => c.CreatedAt)
            .Skip((pageNum - 1) * pageSize)
            .Take(pageSize)
            .Select(c => new CustomerListDto
            {
                Id = c.Id,
                CustomerCode = c.CustomerCode,
                CustomerName = c.CustomerName,
                Industry = c.Industry,
                ContactName = c.ContactName
            })
            .ToListAsync();

        return PagedResult<CustomerListDto>.Of(list, total, pageNum, pageSize);
    }

    public async Task<CustomerListDto?> GetByIdAsync(long id)
    {
        return await _db.Customers.AsNoTracking()
            .Where(c => c.Id == id)
            .Select(c => new CustomerListDto
            {
                Id = c.Id,
                CustomerCode = c.CustomerCode,
                CustomerName = c.CustomerName,
                Industry = c.Industry,
                ContactName = c.ContactName
            })
            .FirstOrDefaultAsync();
    }

    public async Task<long> AddAsync(CustomerCreateDto dto)
    {
        // 校验编码唯一
        var exists = await _db.Customers.AnyAsync(c => c.CustomerCode == dto.CustomerCode);
        if (exists)
            throw new BusinessException("客户编码已存在");

        var entity = new CrmCustomer
        {
            CustomerCode = dto.CustomerCode,
            CustomerName = dto.CustomerName,
            Industry = dto.Industry,
            Region = dto.Region,
            ContactName = dto.ContactName,
            ContactPhone = dto.ContactPhone,
            CreatedAt = DateTime.Now,
            UpdatedAt = DateTime.Now
        };

        _db.Customers.Add(entity);
        await _db.SaveChangesAsync();
        return entity.Id;
    }

    public async Task UpdateAsync(long id, CustomerCreateDto dto)
    {
        var entity = await _db.Customers.FindAsync(id);
        if (entity == null)
            throw new BusinessException("客户不存在");

        entity.CustomerName = dto.CustomerName;
        entity.Industry = dto.Industry;
        entity.ContactName = dto.ContactName;
        entity.UpdatedAt = DateTime.Now;

        await _db.SaveChangesAsync();
    }

    public async Task DeleteAsync(long id)
    {
        var entity = await _db.Customers.FindAsync(id);
        if (entity != null)
        {
            _db.Customers.Remove(entity);
            await _db.SaveChangesAsync();
        }
    }
}
```

---

## 状态流转设计（核心）

每个业务流程都有状态机，用整数表示：

### 报价单状态

```
0 草稿 → 1 待技术评审 → 2 待定价审批 → 3 已发出 → 9 完成
         ↓驳回          ↓驳回
         0 草稿          1 待技术评审
```

### 样品申请状态

```
0 草稿 → 1 待主管审批 → 2 已批准 → 9 完成
         ↓驳回
         0 草稿（带驳回原因）
```

### 批次生产状态

```
0 待排产 → 1 计划中 → 2 生产中 → 3 已完成
```

**代码实现**：在Service里写状态流转方法，校验是否允许流转。

```csharp
public async Task SubmitForReviewAsync(long quotationId, long techReviewerId)
{
    var quote = await _db.Quotations.FindAsync(quotationId);
    if (quote == null || quote.Status != 0)
        throw new BusinessException("报价单状态不正确，无法提交评审");

    quote.Status = 1; // 待评审
    quote.TechReviewBy = techReviewerId;
    await _db.SaveChangesAsync();
}

public async Task TechReviewAsync(long quotationId, long reviewerId, 
    bool approved, decimal cost, string? note)
{
    var quote = await _db.Quotations.FindAsync(quotationId);
    if (quote == null || quote.Status != 1)
        throw new BusinessException("报价单状态不正确");

    quote.TechReviewBy = reviewerId;
    quote.TechReviewDate = DateTime.Now;
    quote.TechReviewCost = cost;
    quote.TechReviewNote = note;
    quote.Status = approved ? 2 : 0; // 通过=待审批，驳回=草稿
    
    await _db.SaveChangesAsync();
}
```

---

## 审批流设计（简化版，适合新手）

先不用 Workflow Core，用字段实现，等P3-P4跑通后再考虑引入。

### 业务表直接加审批字段

```csharp
public class QuoQuotation
{
    // ... 其他字段
    public long? ApprovedBy { get; set; }
    public DateTime? ApprovedAt { get; set; }
    public string? ApproveNote { get; set; }
    public int Status { get; set; }
}
```

### 审批操作接口

```csharp
[HttpPost("{id}/approve")]
public async Task<ApiResult> Approve(long id, [FromBody] ApproveDto dto)
{
    await _quotationService.PricingApproveAsync(id, dto);
    return ApiResult.Success();
}

[HttpPost("{id}/reject")]
public async Task<ApiResult> Reject(long id, [FromBody] ApproveDto dto)
{
    await _quotationService.PricingRejectAsync(id, dto);
    return ApiResult.Success();
}
```

---

## 全局异常处理

```csharp
// Common/BusinessException.cs
public class BusinessException : Exception
{
    public BusinessException(string message) : base(message) { }
}

// Common/GlobalExceptionHandler.cs
public class GlobalExceptionHandler : IMiddleware
{
    public async Task InvokeAsync(HttpContext context, RequestDelegate next)
    {
        try
        {
            await next(context);
        }
        catch (BusinessException ex)
        {
            context.Response.StatusCode = 400;
            await context.Response.WriteAsJsonAsync(ApiResult.Error(ex.Message));
        }
        catch (Exception ex)
        {
            context.Response.StatusCode = 500;
            await context.Response.WriteAsJsonAsync(ApiResult.Error("系统内部错误"));
        }
    }
}

// Program.cs 注册
builder.Services.AddTransient<GlobalExceptionHandler>();
// ...
app.UseMiddleware<GlobalExceptionHandler>();
```

---

## JWT认证配置

### appsettings.json

```json
{
  "Jwt": {
    "Secret": "shibide-cpm-secret-key-2026-must-be-at-least-32-characters-long",
    "Issuer": "CpmServer",
    "Audience": "CpmWeb",
    "ExpirationHours": 24
  }
}
```

### JWT配置类

```csharp
public class JwtSettings
{
    public string Secret { get; set; } = string.Empty;
    public string Issuer { get; set; } = string.Empty;
    public string Audience { get; set; } = string.Empty;
    public int ExpirationHours { get; set; }
}
```

### Program.cs 注册JWT

```csharp
builder.Services.Configure<JwtSettings>(
    builder.Configuration.GetSection("Jwt"));

var jwtSettings = builder.Configuration.GetSection("Jwt").Get<JwtSettings>()!;

builder.Services.AddAuthentication(JwtBearerDefaults.AuthenticationScheme)
    .AddJwtBearer(options =>
    {
        options.TokenValidationParameters = new TokenValidationParameters
        {
            ValidateIssuer = true,
            ValidateAudience = true,
            ValidateLifetime = true,
            ValidateIssuerSigningKey = true,
            ValidIssuer = jwtSettings.Issuer,
            ValidAudience = jwtSettings.Audience,
            IssuerSigningKey = new SymmetricSecurityKey(
                Encoding.UTF8.GetBytes(jwtSettings.Secret))
        };
    });

// ...
app.UseAuthentication();
app.UseAuthorization();
```

---

## 文件上传规范

```csharp
[ApiController]
[Route("api/[controller]")]
public class FileController : ControllerBase
{
    private readonly IWebHostEnvironment _env;

    public FileController(IWebHostEnvironment env)
    {
        _env = env;
    }

    [HttpPost("upload")]
    public async Task<ApiResult<string>> Upload(IFormFile file)
    {
        if (file == null || file.Length == 0)
            throw new BusinessException("文件为空");

        var ext = Path.GetExtension(file.FileName);
        var newName = $"{Guid.NewGuid()}{ext}";
        var dateDir = DateTime.Now.ToString("yyyy/MM/dd");
        var dir = Path.Combine(_env.WebRootPath, "uploads", dateDir);
        
        if (!Directory.Exists(dir))
            Directory.CreateDirectory(dir);

        var filePath = Path.Combine(dir, newName);
        await using var stream = System.IO.File.Create(filePath);
        await file.CopyToAsync(stream);

        return ApiResult<string>.Success($"/uploads/{dateDir}/{newName}");
    }
}
```

> 确保 `Program.cs` 中有 `app.UseStaticFiles();`

---

## 日志规范

.NET内置 `ILogger`，直接在构造函数注入：

```csharp
public class CustomerService : ICustomerService
{
    private readonly CpmDbContext _db;
    private readonly ILogger<CustomerService> _logger;

    public CustomerService(CpmDbContext db, ILogger<CustomerService> logger)
    {
        _db = db;
        _logger = logger;
    }

    public async Task<long> AddAsync(CustomerCreateDto dto)
    {
        _logger.LogInformation("创建客户: {Code}, {Name}", dto.CustomerCode, dto.CustomerName);
        // ...
    }
}
```

---

## 新手开发顺序（每阶段代码文件清单）

### P1 用户登录
1. `Models/SysUser.cs`、`SysRole.cs`、`SysUserRole.cs` — 实体
2. `Data/CpmDbContext.cs` — DbContext
3. `Common/ApiResult.cs`、`BusinessException.cs`、`GlobalExceptionHandler.cs`
4. `DTOs/Auth/LoginRequest.cs`、`LoginResponse.cs`
5. `Services/AuthService.cs` — 登录+Token生成
6. `Controllers/AuthController.cs` — 登录/注册接口
7. `Program.cs` — 注册JWT、CORS、异常处理

### P2 基础数据
1. `Models/CrmCustomer.cs` — 实体
2. `DTOs/Customer/CustomerCreateDto.cs`、`CustomerListDto.cs`
3. `Controllers/CustomerController.cs` — 增删改查分页
4. `Services/CustomerService.cs` — 业务逻辑

### P3 报价流程
1. `Models/QuoOpportunity.cs`、`QuoQuotation.cs`、`QuoQuotationItem.cs`
2. `Controllers/QuotationController.cs` — 创建商机、提交评审、审批
3. `Services/QuotationService.cs` — 状态流转逻辑（核心）

（后续阶段以此类推，每个阶段增加对应模块的Controller/Service/DTO/Model）